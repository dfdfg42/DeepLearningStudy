path: ../dataset          # img/labels 디렉터리들이 들어있는 상위 폴더
train: images/train       # => ../dataset/images/train, labels/train
val:   images/val
test:  images/test        # 선택
nc: 3                     # 클래스 수
names: ["no mask", "improperly", "mask"]
